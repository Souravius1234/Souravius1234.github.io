# Legacy pages

These pages are retained as design references and are not part of the supported site navigation.

- `generic.html` — generic experimental layout with placeholder content
- `elements.html` — component and styling experiment

They were moved out of the repository root to keep public top-level pages limited to active site sections.
